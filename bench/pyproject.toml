[project]
name = "demucs-bench"
version = "0.1.0"
description = "Python reference benchmarks for debugging the Rust Demucs implementation"
requires-python = ">=3.11,<3.13"
dependencies = [
    "torch>=2.2",
    "demucs",
    "safetensors",
    "numpy",
    "soundfile",
]
